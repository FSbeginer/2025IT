import javax.swing.JPanel;
import java.awt.Color;

import javax.swing.Icon;
import javax.swing.JLabel;
import java.awt.Font;
import java.sql.SQLException;

import javax.swing.SwingConstants;
import javax.swing.table.DefaultTableCellRenderer;
import javax.swing.table.DefaultTableModel;
import javax.swing.JScrollPane;
import javax.swing.JTable;

public class PageList extends JPanel {
	public JLabel label;
	public JLabel label_1;
	public JScrollPane scrollPane;
	public JTable table;
	private DefaultTableModel model;

	/**
	 * Create the panel.
	 */
	public PageList() {
		setBackground(new Color(255, 255, 255));
		setSize(778, 431);
		setLayout(null);
		
		label = new JLabel("\uAD6C\uB9E4\uBAA9\uB85D");
		label.setFont(new Font("¸¼Àº °íµñ", Font.BOLD, 20));
		label.setBounds(12, 10, 99, 40);
		add(label);
		
		label_1 = new JLabel("\uCD1D \uAE08\uC561: 0\uC6D0");
		label_1.setHorizontalAlignment(SwingConstants.RIGHT);
		label_1.setFont(new Font("¸¼Àº °íµñ", Font.BOLD, 20));
		label_1.setBounds(276, 10, 490, 40);
		add(label_1);
		
		scrollPane = new JScrollPane();
		scrollPane.setBounds(12, 61, 754, 360);
		add(scrollPane);
		
		table = new JTable() {
			@Override
			public Class<?> getColumnClass(int column) {
				if(column==0)
					return Icon.class;
				return super.getColumnClass(column);
			}
		};
		scrollPane.setViewportView(table);
		
		addModel();
		load();
	}

	private void addModel() {
		model = new DefaultTableModel(" ,»óÇ°¸í,¼ö·®,´Ü°¡,ÇÕ°è,±¸¸Å³¯Â¥".split(","), 0);
		table.setModel(model);
		var render = new DefaultTableCellRenderer();
		render.setHorizontalAlignment(SwingConstants.CENTER);
		for (int i = 1; i < table.getColumnCount(); i++) {
			table.getColumnModel().getColumn(i).setCellRenderer(render);
		}
		table.getTableHeader().setBackground(Color.white);
		table.setRowHeight(80);
	}

	private void load() {
		model.setRowCount(0);
		try (var rs = BF.res("select * from `order` join product using(pno) where uno = "+BF.uno)) {
			while(rs.next()) {
				
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
	}	
}
